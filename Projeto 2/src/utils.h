#ifndef _utils_h
#define _utils_h

#define min(X,Y) (((X) < (Y)) ? (X) : (Y))
#define max(X,Y) (((X) > (Y)) ? (X) : (Y))

typedef unsigned long ulong;

typedef enum {
  true = 0,
  false = 1
} Bool;

#endif
